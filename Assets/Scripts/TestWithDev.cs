using UnityEngine;

public class TestWithDev : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello from Dev Branch");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
